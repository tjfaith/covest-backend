generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id      String      @id @default(autoincrement())
  email   String   @unique
  name    String?
  phone_number  String?   
}